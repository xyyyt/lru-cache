# lru-cache

Implementation of an LRU cache inspired by functools.lru_cache, providing a simple decorator that stores recent results and automatically evicts older ones.

## Dependencies

- python3
- python3-pytest